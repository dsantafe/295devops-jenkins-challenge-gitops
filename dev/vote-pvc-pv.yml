kind: PersistentVolume
apiVersion: v1
metadata:
  name: vote-pv-volume  # Sets PV's name
  labels:
    type: local  # Sets PV's type to local
    app: vote
spec:
  storageClassName: manual
  capacity:
    storage: 5Gi # Sets PV Volume
  accessModes:
    - ReadWriteMany
  hostPath:
    path: "/mnt/app"
---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: vote-pv-claim  # Sets name of PVC
  labels:
    app: vote
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany  # Sets read and write access
  resources:
    requests:
      storage: 5Gi  # Sets volume size
      