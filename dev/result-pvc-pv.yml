kind: PersistentVolume
apiVersion: v1
metadata:
  name: result-pv-volume  # Sets PV's name
  labels:
    type: local  # Sets PV's type to local
    app: result
spec:
  storageClassName: manual
  capacity:
    storage: 5Gi # Sets PV Volume
  accessModes:
    - ReadWriteMany
  hostPath:
    path: "/mnt/app"
---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: result-pv-claim  # Sets name of PVC
  labels:
    app: result
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany  # Sets read and write access
  resources:
    requests:
      storage: 5Gi  # Sets volume size
      